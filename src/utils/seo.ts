const BASE_TITLE = `NextBase | Premium Next.js 13, Supabase, Typescript SAAS boilerplate.`;
export const withBaseTitle = (title: string) => `${title} | ${BASE_TITLE}`;
const BASE_DESCRIPTION = `NextBase`;
export const withBaseDescription = (description: string) =>
  `${description} | ${BASE_DESCRIPTION}`;
